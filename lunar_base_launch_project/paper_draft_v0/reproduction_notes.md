# Reproduction Notes

Run from:

```powershell
G:\国际大物\lunar_base_launch_project
```

Commands:

```powershell
python .\src\run_baseline.py
python .\src\paper_figures.py
python .\src\self_check.py
```

If direct working directory execution has sandbox issues, use absolute paths from `G:\国际大物`:

```powershell
python G:\国际大物\lunar_base_launch_project\src\run_baseline.py
python G:\国际大物\lunar_base_launch_project\src\self_check.py
```

If using absolute paths, run the same `src\paper_figures.py` script after
`src\run_baseline.py` to refresh the enhanced SVG figures.

Expected validation:

```text
self_check passed
```

Generated result directories:

```text
../results/figures
../results/tables
../results/trajectories
```

Draft-specific figure:

```text
figures/fig2_architecture.svg
figures/fig2_architecture_enhanced.svg
```

The original architecture schematic is hand-authored. The enhanced figure set is
generated by `src/paper_figures.py` from the existing CSV/JSON results.

Enhanced paper figures:

```text
../results/figures/architecture_trade_matrix_enhanced.svg
../results/figures/tli_mass_sankey_enhanced.svg
../results/figures/reliability_chain_enhanced.svg
../results/figures/leo_tli_orbit_diagram_enhanced.svg
../results/figures/ascent_dashboard_enhanced.svg
figures/fig2_architecture_enhanced.svg
```

PNG previews:

```text
../results/figures/previews
```

Core result summary:

```text
../results/tables/baseline_summary.json
```

Important generated numbers:

- Recommended architecture: `B_two_leo_rendezvous`.
- 300 km TLI delta-v: about `3.108 km/s`.
- Nominal initial LEO stack: about `96.9 t`.
- Nominal per-launch LEO wet mass: about `48.5 t`.
- ECI ascent terminal altitude: about `306.9 km`.
- ECI ascent terminal inertial speed: about `7.723 km/s`.
- ECI ascent terminal flight path angle: about `-0.02 deg`.
- Baseline mission chain reliability at `R_launch=0.95`: about `0.871`.
- J2 minus spherical terminal altitude difference: about `-2.27 km`.
