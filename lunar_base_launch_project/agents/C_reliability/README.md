# Agent C：可靠性智能体

## 职责

- 建立发动机簇可靠性模型。
- 建立两发全成功与三发 2-out-of-3 可靠性模型。
- 输出可靠性敏感性表和图。

## 当前性能

状态：v0.6 可用。  
强项：能清楚区分发动机簇可靠性、单发火箭可靠性和多发任务可靠性。  
新增：已把两发主方案写成 `R_total = R_launch^2 * R_rendezvous * R_TLI`。  
新增：已对 `R_rendezvous` 和 `R_TLI` 做二维敏感性扫描。  
短板：交会对接和 TLI 可靠性当前仍是参数假设，后续论文中应明确为假设范围。
